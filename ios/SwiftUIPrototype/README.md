# SwiftUI Prototype

This is a deliberately small native spike, not a rewrite.

It should prove whether a SwiftUI version can load the same exported quiz data, browse categories, run a basic quiz flow, score answers, record missed questions, and display bundled anatomy images from `public/drawings`.

Build with:

```bash
npm run ios:swiftui:build
```

